# /test

用途：为组件或函数生成测试并落盘。

## 参数
- `<path>`: 目标文件路径

## 执行流程
1. 读取代码并识别边界场景。
2. 生成测试文件到同级 `__tests__/`。
3. 如条件允许运行测试并返回结果摘要。

## 测试要求
- 覆盖正常路径、边界值、错误路径、交互路径。
- 使用 Vitest + React Testing Library。
- 优先语义化查询（`getByRole`）。
