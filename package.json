{
  "name": "wangai-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "WangAI monorepo for @wangai/cli and @wangai/sync",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:cli": "node ./packages/cli/bin/wangai.js",
    "start:sync": "node ./packages/sync/bin/wangai-sync.js",
    "check:sync": "cd packages/sync && npm run check && npm pack --dry-run",
    "docs:sync": "node ./scripts/sync-docs.js",
    "test": "echo \"No tests yet\""
  }
}
