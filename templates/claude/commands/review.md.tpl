---
description: 审查代码质量，检查类型安全、性能、安全和规范
argument-hint: <文件或目录路径>
---

请审查以下代码：$ARGUMENTS

## 执行步骤

1. 读取指定路径的代码文件
2. 按审查清单逐项分析
3. 输出审查报告（直接输出，不需要写入文件）

## 审查清单

### 🔴 必须关注

**TypeScript 类型安全**
- 是否存在 `any` 类型
- 类型断言（`as`）是否合理
- 函数参数和返回值是否有明确类型
- 是否利用了项目的 `noUncheckedIndexedAccess` 配置

**安全风险**
- 是否有 XSS 风险（`dangerouslySetInnerHTML`）
- 用户输入是否经过校验
- 敏感信息是否暴露在客户端

### 🟡 建议关注

**框架最佳实践**
- Server/Client 组件划分是否正确（默认 Server，需要 hooks 才加 "use client"）
- 数据获取方式是否恰当
- 是否正确使用 `next/image` 和 `next/link`

**性能隐患**
- 是否有不必要的重渲染
- 列表渲染是否有稳定 `key`
- 是否缺少 `React.memo` / `useMemo` / `useCallback`
- 大型对象/函数是否在渲染函数外部定义

### 🟢 可选关注

**代码风格**
- 是否符合项目 CLAUDE.md 中的编码规范
- 命名是否清晰、一致
- 是否有可读性问题

## 输出格式

```
## 审查结果：[文件名]

### 🔴 必须修复
- **[行号]** 问题描述
  ```tsx
  // 修改建议
  ```

### 🟡 建议优化
...

### 🟢 小贴士
...

### 总结
[整体评价，如：代码质量良好，建议修复 xxx]
```

如果代码没有问题，直接输出"✅ 代码审查通过，未发现明显问题"。
